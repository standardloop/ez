#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(int argc, char *argv[])
{
    if (argc <= 1)
    {
        printf("Please provide at least 1 argument\n");
        return EXIT_FAILURE;
    }

    int number_to_factor = atoi(argv[1]);

    int i = 1;

    // linked list
    // while ()
    // {
    // }

    return EXIT_SUCCESS;
}
